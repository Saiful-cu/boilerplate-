/* Express removed — backend is now a MongoDB-only worker and library.
 * The previous HTTP server implementation was intentionally removed. Keep
 * programmatic services (models, service classes) in `src/modules/*` and use
 * `src/cli.ts` / `src/scripts/*` for ad-hoc DB operations.
 */

export { };
