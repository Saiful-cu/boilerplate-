/* Health endpoints removed — backend is a DB worker. Keep this file as a reference
 * for health checks; if you need a health endpoint in future, implement a small
 * HTTP process separately or query Mongo directly.
 */

export { };
